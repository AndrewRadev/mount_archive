#! /usr/bin/env ruby

$: << File.expand_path('../../lib', __FILE__)

require 'mount_archive/fuse_adapter'

if ARGV.count < 1
  puts 'USAGE: mount_archive <archive_name>'
  exit(1)
end

filename = ARGV.shift

archive = MountArchive::FuseAdapter.new(filename)
archive.mount
